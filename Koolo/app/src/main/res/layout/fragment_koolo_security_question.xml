<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/background_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:scaleType="fitXY"
        android:src="@drawable/background"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="37dp"
        android:maxLines="1"
        android:textSize="20sp"
        android:text="Answer Security Question"
        android:id="@+id/security_question_note"
        android:gravity="center_horizontal|top"
        android:textColor="@color/White"
        android:layout_marginTop="50dp"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:layout_marginRight="@dimen/activity_horizontal_margin"
        android:background="@android:color/transparent"
        android:layout_alignParentTop="true" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="37dp"
        android:maxLines="1"
        android:textSize="18sp"
        android:text=""
        android:id="@+id/security_question_text"
        android:layout_below="@+id/security_question_note"
        android:textColor="@color/White"
        android:layout_marginTop="50dp"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:layout_marginRight="@dimen/activity_horizontal_margin"
        android:layout_marginBottom="@dimen/activity_horizontal_margin"
        android:background="@android:color/transparent" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:id="@+id/security_question_answer_edit_text"
        android:layout_below="@+id/security_question_text"
        android:layout_marginTop="50dp"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:layout_marginRight="@dimen/activity_horizontal_margin"
        android:layout_marginBottom="@dimen/activity_horizontal_margin"
        android:imeOptions="actionDone"
        android:maxLines="1"
        android:layout_gravity="left|center_horizontal"
        android:inputType="textNoSuggestions"
        android:textColor="@android:color/black"
        android:background="@drawable/drawable_edit_text_border"
        android:alpha="0.7"
        android:textSize="18sp"/>

</RelativeLayout>
